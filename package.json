# 在项目根目录执行
cat > package.json << 'EOF'
{
  "name": "mr-lee-design-portfolio",
  "version": "1.0.0",
  "description": "静态设计作品集网站",
  "main": "index.html",
  "scripts": {
    "preinstall": "exit 0",
    "install": "exit 0",
    "build": "echo '无需构建' && exit 0"
  },
  "private": true
}
EOF